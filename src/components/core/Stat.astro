---
const { label, value } = Astro.props;
---
<div class="flex justify-between items-center p-4 bg-secondary/50 rounded-xl">
  <span>{label}</span>
  <span class="text-2xl font-bold text-primary">{value}</span>
</div>
